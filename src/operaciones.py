"""Recibe una lista y devuelve otra con los numeros pares unicamente"""


def filtrar_pares(lista):
    """Recibe una lista y devuelve otra con los números pares únicamente"""
    pares = [i for i in lista if i % 2 == 0]
    return pares
    



